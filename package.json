{
  "name": "js-interview-questions",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "install": "concurrently \"npm i --prefix frontend\" \"npm i --prefix backend\"",
    "dev": "concurrently \"wait-on http://localhost:5000/questions && npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  }
}
